-- Fazer uma função que resolva o seguinte problema (a^2) + (3*b)

-- Faz entrada dos números 'a' e 'b'

a, b = io.read('*n','*n')

-- Faz a função que resolve o problema (a^2) + (3*b), e printa a resposta:

 function aeb(a, b)
	print((a^2) + (3*b))
end

-- E por último, chamar a função:

aeb(a,b)

-- Prontinho!
